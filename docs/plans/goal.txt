Ship v0.4.1 of Arcana: rebase retrieve.factRetrieval onto KyberBot's empirical implementation per ADR 011 (port-first). Do NOT execute npm publish. Internal-logic change only — no contract additions; shapes unchanged. Plan: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-21-fact-retrieval-rebase.md. KB source of truth (READ FIRST): /Users/davidcruwys/dev/kybernesis/kyberbot/packages/cli/src/brain/fact-retrieval.ts. Done when ALL of: (1) factRetrieval in packages/arcana-core/src/retrieve/index.ts ports KB's 4-layer flow (FTS → entity → graph → bridge); structure visibly matches the KB file; (2) no call to structured.getNeighbors({type:'memory',...}) from inside factRetrieval (Arcana-invented memory-expansion removed); structured.getNeighbors stays in the provider for other callers including entity-graph traversal in the new flow; (3) factRetrieval uses structured.searchFulltext + listEntities + getFactsForEntity in the ported flow; (4) packages/arcana-core/src/retrieve/index.test.ts updated — tests cover each KB-faithful layer (FTS, entity, graph, bridge); Arcana-invented memory-expansion test removed or rewritten; envelope/empty/topK tests still pass; parity-harness smoke test added using runParityHarness from @kybernesis/arcana-testkit/parity, same-impl baseline asserting passes:true; (5) `bun run build` exits 0; (6) `bun run test` exits 0 with ≥ 260 tests; (7) all 5 packages bumped 0.4.0 → 0.4.1; (8) CHANGELOG.md has dated v0.4.1 section referencing ADR 011, naming KB source file, listing removed (Arcana-invented memory-expansion) and replacement (KB-faithful 4-layer flow); (9) .mochaccino/data/06-kernel-methods.json factRetrieval entry updated + test count bumped; 03-publish-pipeline.json gains v0.4.1 lane (status not_started); (10) .mochaccino/views/kernel-methods.html factRetrieval row + tagline + counter bumped; index.html stats + done-strip updated; publish-pipeline.html chips + new v0.4.1 lane added (tag green after tagging, publish rows not_started); (11) ARCANA → KBOT entry dated 2026-05-21 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md announcing v0.4.1 as second ADR 011 application, parity expectation 100%, default action bump deps to ^0.4.1; (12) two commits on main — feat (rebase + tests + docs + mochaccino + comms) then chore (version bump); (13) git tag v0.4.1 created and pushed; (14) `git push origin main` and `git push origin v0.4.1` executed; (15) Findings appendix in the plan populated with concrete resolutions for port-time judgment calls (layer-boundary semantics, fact-graph traversal against Arcana's facts+edges schema, bridge layer impl choice, scoring weights, query tokenization, empty-result handling) — each resolution citing KB file:line and Arcana code location. Check: `npm view @kybernesis/arcana-core@0.4.1 version` returns 404, `git ls-remote --tags origin v0.4.1` returns the tag, `bun run test` exits 0. Out of scope: sleep pipeline, arcana-provider-llm-claude-code, arcana-provider-llm-http, arcana-provider-postgres, block-zone facades (done v0.3.1), getEntityProfile generalisation (additive per ADR 011), resurrecting graph-BFS-via-getNeighbors in factRetrieval (deferred to v2), FactRetrievalInput/HybridSearchResult/StructuredStore contract changes, npm publish itself, sleep stubs, deleteMemory, stats, updateBlock, any file in /Users/davidcruwys/dev/kybernesis/kyberbot or /Users/davidcruwys/dev/kybernesis/kybernesis-brain. Stop after 50 turns if not converged.
