Ship v0.5.0: new package @kybernesis/arcana-provider-llm-claude-code — subprocess-based LLM provider porting KyberBot's claude.ts → completeSubprocess path per ADR 011 (port-first) + ADR 012 (LLM architecture). Do NOT execute npm publish. Plan: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-21-llm-claude-code-provider.md. KB source of truth (READ FIRST): /Users/davidcruwys/dev/kybernesis/kyberbot/packages/cli/src/claude.ts. Done when ALL of: (1) packages/arcana-provider-llm-claude-code/ exists — package.json (version 0.5.0, dep @kybernesis/arcana-contracts workspace:*), tsconfig.json, README.md, src/index.ts, src/index.test.ts — template: packages/arcana-provider-libsql/; (2) src/index.ts exports createClaudeCodeLLMProvider(opts?) returning an LLMProvider; ClaudeCodeProviderOptions = {binary? default 'claude', defaultModel? 'haiku'|'sonnet'|'opus' default 'haiku', cwd?, logger? optional}; (3) implementation uses child_process.spawn with KB's verified invocation pattern (read claude.ts to confirm argv vs stdin); model IDs match KB claude.ts:64-68 — haiku→'claude-haiku-4-5', sonnet→'claude-sonnet-4-6', opus→'claude-opus-4-7'; (4) tests use vi.mock('node:child_process') so no real claude spawns; ≥ 8 tests covering happy-path, model mapping, default model, cwd, system prompt, non-zero exit, ENOENT; (5) root tsconfig.json adds project reference to new package; (6) `bun run build` exits 0; (7) `bun run test` exits 0 with ≥ 270 tests; (8) all 6 packages at 0.5.0 (existing 5 bump 0.4.1→0.5.0 to align); (9) CHANGELOG.md dated v0.5.0 section: new package, references ADR 011 + ADR 012, names KB source, notes `claude -p` deprecation horizon (contract stays stable); (10) packages/arcana-provider-llm-claude-code/README.md with usage example showing createArcana({llm: createClaudeCodeLLMProvider()}); (11) .mochaccino/data — 02-package-graph.json adds new node; 03-publish-pipeline.json package_count 5→6 + packages_to_publish includes new pkg + v0.5.0 lane (not_started); 04-contracts-surface.json notes LLMProvider has concrete impl; 06-kernel-methods.json test count bumped; (12) .mochaccino/views — index.html, kernel-methods.html, package-graph.html (6-pkg + new node), publish-pipeline.html (6 chips + v0.5.0 lane) all refreshed; (13) ARCANA → KBOT entry dated 2026-05-21 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md — v0.5.0, new package, default action optional (KB can swap claude.ts to consume Arcana's wrapper; 100% parity expectation per ADR 011); (14) two commits on main — feat (package + tests + docs + mochaccino + comms + workspace) then chore (6 version bumps to 0.5.0); (15) git tag v0.5.0 created and pushed; (16) `git push origin main` and `git push origin v0.5.0` executed; (17) Findings appendix populated with CONCRETE resolutions for: subprocess invocation pattern (argv vs stdin), model-flag syntax, maxTokens plumbing, system prompt mechanism, cwd resolution, error surface, `-p` deprecation horizon note — each citing KB file:line + Arcana code location. Check: `npm view @kybernesis/arcana-provider-llm-claude-code@0.5.0 version` returns 404, `git ls-remote --tags origin v0.5.0` returns tag, `bun run test` exits 0. Out of scope: arcana-provider-llm-http (separate per ADR 012), Agent SDK in-process mode (disabled in KB for memory leak), streaming/onChunk, loopDetection, reranker utility (sleep sprint), sleep pipeline impl, v2 factRetrieval, arcana-provider-postgres, npm publish itself, KyberBot/Brain repo files. Stop after 50 turns if not converged.
