Ship v1.2.0: System Health Phase 1 — 8 production-blockers from docs/SYSTEM-HEALTH.md + 2 doc closeouts. NO npm publish. Plan: docs/plans/2026-05-22-system-health-phase1.md. Done when ALL of: (1) Entity normalisation — ingest.extractFacts + command.recordFact + maintain.observeConversations lowercase+trim entities before storeFact; libsql connect() runs idempotent migration lowercasing existing entities_json (meta row schema_version=2 prevents re-run); FactSchema.entities doc-comment notes lowercased-canonical; (2) latestOnly?: boolean (default true) added to StructuredStore.getFactsForEntity + listMemories (via MemoryFilter); libsql + testkit fake updated; (3) transaction<T>(fn) primitive added to StructuredStore contract; libsql wraps db.transaction passing self as tx; testkit fake calls fn(this); ingest.storeMemory wraps memory+chunks atomically; (4) deleteEntity cascades to edges + insights + entity_profile (NOT facts per multi-entity schema); wrapped in transaction(); (5) getNeighbors honors hops 1-5 via WITH RECURSIVE in libsql; throw outside range; testkit fake does BFS-from-seed; returns DISTINCT NodeRef[] excluding seed; (6) runSleepPipeline single-flight: closure-level running:Promise|null; concurrent calls return same in-flight promise; cleared in finally; (7) Partial-failure checkpoint: Map<SleepStep,'partial'|'complete'>; errors[]>0 marks 'partial'; resume retries non-'complete'; SleepRunResult gains partialSteps:SleepStep[]; (8) docs/decisions/README.md lists ADRs 008-013; (9) ADR 010 status → "Superseded by ADR 011 + v1.1.0" with 2-line epilogue; (10) ADR 011 gains § "Status of parity verification" (table: hybridSearch/factRetrieval/sleep × Target/Measured/Gap; initial 100%/pending KB fixtures/TBD); (11) integration test packages/arcana-core/src/maintain/e2e-pipeline.test.ts: seed chat memory + entity, run observe→rebuildUserProfile→runReasoning against testkit fake, assert ≥1 insight; (12) all 6 packages at v1.2.0; (13) bun run build exits 0; (14) bun run test exits 0 with ≥350 tests (328 baseline + ~22 new); (15) CHANGELOG v1.2.0 enumerates 8 fixes + 2 doc closeouts + cites docs/SYSTEM-HEALTH.md; (16) Mochaccino refreshed — 06-kernel-methods.json test count, 03-publish-pipeline.json v1.2.0 lane (not_started), index.html stat; (17) ARCANA→KBOT entry dated 2026-05-22 appended to ~/dev/kybernesis/.comms/arcana-kyberbot.md: v1.2.0 contract additions (transaction + latestOnly + getNeighbors hops), behavior change (entities now lowercase at storage), default KBOT action: re-run parity harness; (18) two commits (feat: 8 fixes + 2 doc closeouts; chore: v1.2.0 bumps); (19) git tag v1.2.0 + push main + push tag; (20) Findings appendix populated with concrete resolutions citing file:line for each item + surprises. Verify: npm view @kybernesis/arcana-contracts@1.2.0 returns 404; git ls-remote --tags origin v1.2.0 returns tag; bun run test exits 0. Out of scope: Phase 2/3 audit items, SPEC/README/full mochaccino refresh (separate docs PR), npm publish, KyberBot/Brain repo files, postgres/llm-http providers, ingestDocument impl. Stop after 60 turns if not converged.
