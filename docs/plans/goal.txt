Ship v1.2.0: System Health Phase 1 — 8 production-blocker fixes from docs/SYSTEM-HEALTH.md + 2 doc closeouts. Do NOT execute npm publish. Plan: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-22-system-health-phase1.md. Audit source: /Users/davidcruwys/dev/kybernesis/arcana/docs/SYSTEM-HEALTH.md. Done when ALL of: (1) Entity normalisation — ingest.extractFacts + command.recordFact + maintain.observeConversations lowercase+trim entities before storeFact; libsql connect() runs idempotent migration lowercasing existing entities_json (mark schema_version=2 in a meta row so it doesn't run twice); FactSchema.entities doc-comment notes lowercased-canonical convention; (2) is_latest filter — StructuredStore.getFactsForEntity AND listMemories (via MemoryFilter) accept latestOnly?: boolean default true; libsql + testkit fake impls updated; (3) transaction<T>(fn) primitive added to StructuredStore contract; libsql impl wraps db.transaction passing self as tx; testkit fake just calls fn(this); ingest.storeMemory wraps memory+chunks atomically via the new primitive; (4) deleteEntity cascades to edges + insights + entity_profile (NOT facts — facts are multi-entity per v1.0.0 schema); cascade wrapped in transaction(); (5) getNeighbors honors hops 1-5 via WITH RECURSIVE in libsql; throw outside range; testkit fake implements BFS-from-seed traversal; return DISTINCT NodeRef[] excluding seed; (6) runSleepPipeline single-flight guard — closure-level running:Promise<SleepRunResult>|null in createMaintain; concurrent calls return the same in-flight promise; cleared in finally; (7) Partial-failure checkpoint state — Map<SleepStep,'partial'|'complete'>; step returning errors[] non-empty marks 'partial'; resume:true re-attempts steps where checkpoint !== 'complete'; SleepRunResult gains partialSteps:SleepStep[] field; (8) docs/decisions/README.md lists ADRs 008-013 mirroring existing 001-007 format; (9) docs/decisions/010-sleep-pipeline-step-reconciliation.md status changed to "Superseded by ADR 011 + v1.1.0" with 2-line epilogue explaining port-first resolution; (10) docs/decisions/011-port-first-improve-later.md gains §"Status of parity verification" section with table (rows: hybridSearch, factRetrieval, sleep pipeline; columns: Target/Measured/Gap; initial values 100%/pending KB fixtures/TBD); (11) integration test packages/arcana-core/src/maintain/e2e-pipeline.test.ts: seed chat memory + entity, run observe → rebuildUserProfile → runReasoning sequentially against testkit fake structured store, assert ≥1 insight produced; (12) all 6 packages at v1.2.0; (13) `bun run build` exits 0; (14) `bun run test` exits 0 with ≥350 tests (328 baseline + ~22 new across the 8 items); (15) CHANGELOG v1.2.0 section enumerates 8 fixes + 2 doc closeouts + references docs/SYSTEM-HEALTH.md as the driving audit; (16) Mochaccino refreshed — 06-kernel-methods.json test count bumped, 03-publish-pipeline.json gets v1.2.0 lane (not_started), index.html test-count stat updated to new count; (17) ARCANA→KBOT entry dated 2026-05-22 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md: v1.2.0, contract additions (transaction primitive + latestOnly default + getNeighbors hops 1-5), behavior-visible change (fact entities now lowercase at storage), KBOT default action: re-run parity harness against v1.2.0 to confirm no regression; (18) two commits on main (feat: 8 fixes + 2 doc closeouts; chore: v1.2.0 bumps); (19) git tag v1.2.0 + git push origin main + git push origin v1.2.0; (20) Findings appendix in the plan doc populated with concrete resolutions citing file:line for each item + any surprises encountered. Verify: npm view @kybernesis/arcana-contracts@1.2.0 version returns 404; git ls-remote --tags origin v1.2.0 returns tag; bun run test exits 0. Out of scope: Phase 2/3 audit items (19 strong recs + hygiene), SPEC.md/README.md/full mochaccino refresh (separate docs PR), npm publish itself, KyberBot/Brain repo files, postgres/llm-http providers, ingestDocument impl. Stop after 60 turns if not converged.
