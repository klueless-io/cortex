Ship v0.5.0 of Arcana: new package @kybernesis/arcana-provider-llm-claude-code — subprocess-based LLM provider porting KyberBot's claude.ts → completeSubprocess path per ADR 011 (port-first) + ADR 012 (LLM architecture). Do NOT execute npm publish. Plan: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-21-llm-claude-code-provider.md. KB source of truth (READ FIRST): /Users/davidcruwys/dev/kybernesis/kyberbot/packages/cli/src/claude.ts. Done when ALL of: (1) packages/arcana-provider-llm-claude-code/ exists with package.json (name @kybernesis/arcana-provider-llm-claude-code, version 0.5.0, dependency on @kybernesis/arcana-contracts workspace:*), tsconfig.json, README.md, src/index.ts, src/index.test.ts — mirroring the existing provider-package shape (use packages/arcana-provider-libsql/ as template); (2) src/index.ts exports createClaudeCodeLLMProvider(opts?: ClaudeCodeProviderOptions) returning an LLMProvider conforming to the @kybernesis/arcana-contracts interface; ClaudeCodeProviderOptions = {binary?: string default 'claude', defaultModel?: 'haiku'|'sonnet'|'opus' default 'haiku', cwd?: string, logger?: optional}; (3) implementation uses child_process.spawn with the verified-from-KB invocation pattern (read claude.ts to confirm argv vs stdin pattern); model shorthand maps to KB's exact model IDs: haiku→'claude-haiku-4-5', sonnet→'claude-sonnet-4-6', opus→'claude-opus-4-7' (KB claude.ts:64-68); (4) tests use vi.mock('node:child_process') so no real claude subprocess spawns; coverage includes happy-path, model mapping, default model fallback, cwd honored, system prompt included, non-zero exit handled, ENOENT handled; ≥ 8 tests for this package; (5) root tsconfig.json adds a project reference to the new package; (6) `bun run build` exits 0 across the workspace; (7) `bun run test` exits 0 with ≥ 270 tests; (8) all 6 packages (5 existing + new one) at version 0.5.0 — existing 5 bump from 0.4.1 → 0.5.0 to align; (9) CHANGELOG.md has dated v0.5.0 section explaining: new package, references ADR 011 + ADR 012, naming KB source file, noting `claude -p` deprecation horizon for future internals migration (contract stays stable); (10) packages/arcana-provider-llm-claude-code/README.md with usage example showing createArcana({llm: createClaudeCodeLLMProvider()}); (11) .mochaccino/data/02-package-graph.json adds the new package node; 03-publish-pipeline.json package_count 5→6, packages_to_publish array includes new package, v0.5.0 lane added (status not_started — publish handed back to user); 04-contracts-surface.json notes LLMProvider now has a concrete impl shipping; 06-kernel-methods.json test count bumped; (12) .mochaccino/views — index.html (test count + tagline + done-strip entry), kernel-methods.html (test count + tagline), package-graph.html (6-package count + new node + tagline), publish-pipeline.html (6 chips + v0.5.0 lane); (13) ARCANA → KBOT entry dated 2026-05-21 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md announcing v0.5.0 with the new package, default action optional (KyberBot can swap its claude.ts to consume Arcana's wrapper when convenient, 100% parity expectation per ADR 011); (14) two commits on main — feat (new package + tests + docs + mochaccino + comms + workspace integration) then chore (all 6 version bumps to 0.5.0); (15) git tag v0.5.0 created and pushed; (16) `git push origin main` and `git push origin v0.5.0` executed; (17) Findings appendix in the plan populated with CONCRETE resolutions for subprocess invocation pattern (argv vs stdin), model-flag syntax, maxTokens plumbing, system prompt mechanism, cwd resolution, error surface, -p deprecation horizon note — each citing KB file:line and Arcana code location. Check: `npm view @kybernesis/arcana-provider-llm-claude-code@0.5.0 version` returns 404 (publish NOT executed), `git ls-remote --tags origin v0.5.0` returns the tag, `bun run test` exits 0. Out of scope: arcana-provider-llm-http (separate sprint per ADR 012), Agent SDK in-process mode (disabled in KB for memory leak reasons), streaming/onChunk, loopDetection, reranker utility wiring (sleep pipeline sprint), sleep pipeline impl, v2 factRetrieval (Finding 0 work), arcana-provider-postgres, npm publish itself, any KyberBot or Brain repo file. Stop after 50 turns if not converged.
